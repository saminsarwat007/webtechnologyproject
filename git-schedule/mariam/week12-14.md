# Mariam — Week 12-14 (15 Jun – 5 Jul 2026)
**Branch:** `feature/docs-and-final`  
**Goal:** project documentation, the **Modules & Ownership** section, presentation prep.

---

## Day 1 — Mon 15 Jun
Top-level README sections.

**Files touched:**
- `README.md` — fill **Team**, **Tech Stack**, **Prerequisites**, **Setup**, **Default Login Credentials**.

```bash
git checkout -b feature/docs-and-final
git add README.md
git commit -m "docs: README team, stack, setup, credentials"
git push -u origin feature/docs-and-final
```

---

## Day 2 — Wed 24 Jun
**Modules & Ownership** + **API Reference** sections.

**Files touched:**
- `README.md` — add the **Modules & Ownership** table (M1–M6 with owner, controllers, frontend views) so the lecturer can see CRUD distribution at a glance. Add a complete **API Reference** with method/path/auth/description for every endpoint, grouped by module.
- `README.md` — update **Project Structure** to show `backend/src/Modules/<Module>/` and `backend/src/Support/Json.php`.

```bash
git add README.md
git commit -m "docs: modules & ownership table + API reference"
git push origin feature/docs-and-final
```

---

## Day 3 — Fri 3 Jul
Final UI consistency pass + presentation notes.

**Files touched:**
- `frontend/src/components/NavBar.vue` — fix overflow on long names; add active link styling.
- `frontend/src/App.vue` — consistent page padding container.
- `README.md` — add **Demo script** appendix (login → browse → apply → admin review → analytics).

```bash
git add frontend/src/components/NavBar.vue frontend/src/App.vue README.md
git commit -m "docs+ui: final consistency pass and demo script"
git push origin feature/docs-and-final
```

> **Open PR:** `feature/docs-and-final` → `main`. Title: *"Final docs: modules ownership, API reference, demo script"*.
