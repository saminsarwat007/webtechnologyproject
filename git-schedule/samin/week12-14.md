# Samin — Week 12-14 (15 Jun – 5 Jul 2026)
**Branch:** `feature/polish-jobs-apps`  
**Goal:** UX polish for M2/M3 — empty states, transitions, mobile responsiveness.

---

## Day 1 — Mon 15 Jun
Empty states with illustrations.

**Files touched:**
- `frontend/src/views/student/BrowseJobs.vue` — empty state "No jobs match your filters" with reset CTA.
- `frontend/src/views/student/MyApplications.vue` — empty state "You haven't applied to any jobs yet" with link to browse.
- `frontend/src/views/admin/ManageJobs.vue` — empty state "No jobs posted yet" with "Post your first job" CTA.

```bash
git checkout -b feature/polish-jobs-apps
git add frontend/src/views/student/BrowseJobs.vue frontend/src/views/student/MyApplications.vue frontend/src/views/admin/ManageJobs.vue
git commit -m "feat(ui): empty states for jobs and applications views"
git push -u origin feature/polish-jobs-apps
```

---

## Day 2 — Wed 24 Jun
Page transitions + loading skeletons.

**Files touched:**
- `frontend/src/App.vue` — wrap `<RouterView>` in a `<Transition name="fade">` for soft page changes.
- `frontend/src/assets/main.css` — `.fade-enter-active`, `.fade-leave-active` keyframes.
- `frontend/src/views/student/BrowseJobs.vue` — replace generic spinner with skeleton card grid while loading.

```bash
git add frontend/src/App.vue frontend/src/assets/main.css frontend/src/views/student/BrowseJobs.vue
git commit -m "feat(ui): fade page transitions + skeleton loading"
git push origin feature/polish-jobs-apps
```

---

## Day 3 — Fri 3 Jul
Mobile responsiveness audit for M2/M3 views.

**Files touched:**
- `frontend/src/views/student/BrowseJobs.vue` — `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`, filter row stacks on mobile.
- `frontend/src/views/student/MyApplications.vue` — table → card list under `md`.
- `frontend/src/views/admin/ManageJobs.vue` — table → card list under `md`; modals fill full width on mobile.

```bash
git add frontend/src/views/student/BrowseJobs.vue frontend/src/views/student/MyApplications.vue frontend/src/views/admin/ManageJobs.vue
git commit -m "feat(ui): mobile-responsive layouts for M2/M3"
git push origin feature/polish-jobs-apps
```

> **Open PR:** `feature/polish-jobs-apps` → `main`. Title: *"M2/M3 polish: empty states, transitions, mobile responsive"*.
